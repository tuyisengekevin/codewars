function maxRot(n) {
let arr=[n];
  let str=n.toString()
  for(let i=0;i<str.length-1;i++){
    let fix=str.slice(0,i)
    let right=str.slice(i)
    let rot=right.slice(1)+right[0]
     str=fix+rot
    arr.push(Number(str))
  }
  
  return Math.max(...arr)
}

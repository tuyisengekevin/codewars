function evenLast(numbers) {
    let ans=0;
 if(numbers.length===0) {return 0;}
  else{
      for(let i=0;i<numbers.length;i+=2){
          ans =ans+(numbers[i]);
      }
 return ans*numbers[numbers.length-1] }
 
}
console.log(evenLast([1,2,4,5,3]))
